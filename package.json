{
  "name": "beaverbot",
  "version": "1.0.0",
  "scripts": {
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "dotenv": "^17.3.1"
  },
  "engines": {
    "pnpm": ">=10.0.0"
  }
}
